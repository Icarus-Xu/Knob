# 实车验证结论（持续更新）

> 来源：`evidence/log_2026*.txt`（车机端 App 内日志，无 adb 环境下的唯一观测记录）。
> ⚠️ 原始日志**不入公开仓库**，只留在本地工作区；本文即是它们的结论汇总，可独立阅读。
> 测试 App：CarProbe（Knob 壳的前身）。

## 结论速览

| # | 结论 | 证据 |
|---|------|------|
| 1 | `BYDAUTO_AC_COMMON` / `BYDAUTO_SETTING_COMMON` 是 **dangerous 级**（protectionLevel=1），运行时申请后**可被授予** ✅ | log_20260827_121155（保护级别探测）、log_20260827_140138（授权成功） |
| 2 | `BYDAUTO_AC_GET/SET`、`BYDAUTO_SETTING_GET/SET` 是 **signature 级**（protectionLevel=2），普通签名 App **不被授予** ❌ | log_20260827_121155、log_20260827_140138（检查权限） |
| 3 | 车控写操作被系统服务端拒绝：`SecurityException - [setIntArray] permission deny!`（setAcTemperature / setAcTemperatureControlMode） | log_20260827_140138 |
| 4 | `hasFeature` 查询被拒：`[getInt] permission deny!` → 设备初始化时无法探测 feature | log_20260827_140138 / 143325 |
| 5 | 座椅通风调用抛 `InvocationTargetException: null`（`setSeatVentilatingState`，BydHal.kt:183）→ 参数或权限未达 HAL | log_20260827_140138 / 143325 |
| 6 | 读空调温度返回 `-2147482645`（BYDAUTO_COMMAND_RESULT 参数无效/不可读） | log_20260827_140138 / 143325 |
| 7 | 一条非官方的底层直写调用方式抛 `NoSuchMethodException`（`BYDAutoAcDevice.set [int, class[I, class[I]`）→ 该方法签名与运行时 SDK 对不上，此通路已从代码中移除 | log_20260827_140138 |
| 8 | 未申请权限时初始化设备直接 `SecurityException - Neither user 10094 nor current process has ...` → 必须走运行时授权 | log_20260826_212555 |

## 关键推论

- **第三方普通签名 App 无法直接 BYDAUTO 车控**：GET/SET 权限是 signature 级（已实测）。能直接控车的应用都持有车机厂商签发的签名。
- ⚠️ **「包名白名单」是推断，尚未验证**：目前只证实了签名级权限这一道门，服务端是否**另外**按包名白名单校验没有证据。若白名单存在，`cn.icarus.knob` 拿到签名也可能被拒 —— 这是壳+DEX 架构的最大前提性风险，签名版装车时优先验证。
- 由此确定架构方向：**壳用车机厂商签名签一次（进程 UID 继承权限）→ 业务逻辑放 plugin.dex 热加载**。签名申请正在进行中。
- ❌ **无障碍模拟点击兜底路线已弃用**（2026-08-28 决策）：不再作为拿不到签名时的退路，全押官方签名路线。无障碍服务在本项目中**只用于捕获蓝牙 HID 按键**，不用于模拟点击车机 UI。

## 返回码速查（BYDAUTO_COMMAND_RESULT）

| 返回码 | 含义 |
|--------|------|
| 0 | 成功 |
| -2147482645 | 参数无效 |
| -2147482646 | 超时 |
| -2147482647 | 忙 |
| -2147482648 | 失败 |

## 更新记录

- 2026-08-27：4 份日志结论汇总（log_20260826_212555 / 121155 / 140138 / 143325）。
- 2026-08-28：把「包名白名单」降级为未验证推断；记录无障碍模拟点击兜底路线弃用。
